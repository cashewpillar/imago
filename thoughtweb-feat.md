
new features
- be able to add custom fields for faster prototyping (behaves like normal tags, its just liek a different collection of tag, a named one-- because current tags are like freeform of sorts)
    - e.g. user can add location field but show in graph as tear drop shape svg (unlike tags which are circle)
    - user can add 7 custom fields max and call it whatever he wants and add "tags" that only belong to that field (so we isolate from the default freeform tags, and the tags from other fields)
    - existing shapes + assign different shapes per custom field
        - fill circle = tags
        - outline circle = entries
        - custom tag a = tear drop
        - custom tag a = square
        - custom tag a = diamond
        - custom tag a = triangle
        - custom tag a = pentagon
        - custom tag a = star
        - custom tag a = asterisk
- when viewing a thought entry from graph, it should open it as a form-like (default view) and allow ediiting and saving
-  